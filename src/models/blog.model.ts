export type BlogDbModel = {
    _id: string;
    name: string;
    description: string;
    websiteUrl: string
}

export type BlogModel = {
    id: string;
    name: string;
    description: string;
    websiteUrl: string;
}
